# قائمة فحص ما قبل الإطلاق الفعلي

هذا الملف تجميع عملي لحالة النشر الحالية على Railway ومتغيرات البيئة المطلوبة/الاختيارية.
القسم التالي مباشرة هو **المصدر الوحيد المعتمَد للحالة الحالية** — كل الأقسام بعده منقولة
تحت "ملاحظات تحقّق تاريخية" وتفضل كما هي بتاريخها الأصلي لغرض التتبّع فقط، ومفيش أي رقم
قديم فيها (زي "883/883" أو "990/990") يتقرأ كحالة حالية.

## 0. الحالة الحالية للإنتاج (CURRENT PRODUCTION STATE) — آخر تحقّق: ٢٠٢٦-٠٩-٢٤

| البند | القيمة | كيف اتحقّقنا |
|---|---|---|
| آخر commit على `main` | `7915495` | `git log` مباشر |
| آخر ترحيلة (migration) | `0063_default_store_settings_row.sql` | قراءة مباشرة لمجلد `server/migrations/` |
| عدد الترحيلات الكلي | 63 | عدّ ملفات `server/migrations/*.sql` |
| اختبارات السيرفر | **1104/1104 ناجحة** — تم التحقق **على قاعدة بيانات جديدة تماماً** (مش قاعدة التطوير القديمة) مرتين متتاليتين مستقلتين، بالإضافة لأربع تشغيلات سابقة على قاعدة التطوير | `npx vitest run` بعد `CREATE DATABASE` فاضية + `tsx src/migrate.ts` |
| بناء المتجر (storefront) | ✅ نظيف | `npm run build` (الجذر) |
| بناء السيرفر | ✅ نظيف (`tsc` صارم) | `npm run build --prefix server` |
| بناء لوحة التحكم (admin) | ✅ نظيف | `npm run build --prefix admin` |
| GitHub CI (`main`, آخر push) | ✅ **SUCCESS فعلياً مؤكَّد** لـ commit `7915495` (run `36059563853`، `status: completed`, `conclusion: success`) | `GET /repos/.../actions/runs?head_sha=...` عبر GitHub API مباشرة |
| نشر Railway (`endearing-clarity`/`Alaa-eldin-`) | ✅ **SUCCESS فعلياً مؤكَّد** لنفس الـ commit (deployment `ea0030f0-...`، `status: SUCCESS`) | Railway API مباشر (`list-deployments`) |
| `GET /health` | ⚠️ **NOT DIRECTLY VERIFIED** — طلب HTTP مباشر لرابط الإنتاج محجوب من بيئة هذه الجلسة (`EGRESS_BLOCKED`/`connect_rejected`، مؤكَّد بمحاولة فعلية). الدليل غير المباشر فقط: `healthcheckPath: /health` في `railway.json` هو ما يعتمد عليه Railway قبل تعليم أي نشر `SUCCESS` | محاولة `curl`/`WebFetch` فعلية فشلت بحجب شبكة صريح |
| Cloudinary | ✅ CONFIGURED | لوج إقلاع فعلي (`startup_config_summary`) بعد آخر نشر ناجح |
| WhatsApp (رسائل + قالب تأكيد الطلب) | ✅ CONFIGURED | نفس المصدر |
| WhatsApp — Railway Cron لإعادة محاولة الإشعارات الفاشلة | ❌ **NOT CONFIGURED** — لا توجد أي خدمة Cron على المشروع | `mcp__Railway__list-services`: خدمتان بس (`Postgres`، `Alaa-eldin-`) |
| Web Push (VAPID) | ✅ CONFIGURED | لوج إقلاع فعلي |
| Turnstile (CAPTCHA) | ✅ CONFIGURED | لوج إقلاع فعلي |
| مُرسِل البريد الإلكتروني (`RESEND_FROM`) | ❌ **لسه على قيمة sandbox الافتراضية** (`onboarding@resend.dev`) — مش دومين مُتحقق منه، غير جاهز لإطلاق فعلي لمستخدمين حقيقيين | قراءة مباشرة لمتغيرات Railway |
| Railway Cron — انتهاء صلاحية نقاط الولاء | ❌ **NOT CONFIGURED** | نفس تأكيد `list-services` |
| Railway Cron — تذكير السلة المهجورة | ❌ **NOT CONFIGURED** | نفس تأكيد `list-services` |
| Railway Cron — تطبيق الأسعار المجدولة | ❌ **NOT CONFIGURED** | نفس تأكيد `list-services` |
| نسخة احتياطية للإنتاج | ❌ **NOT VERIFIED** — لا توجد بيانات اعتماد وصول مباشر لقاعدة بيانات الإنتاج من هذه الجلسة | راجع القسم 8 التاريخي |
| اختبار الدخان الإنتاجي (smoke test) | ⚠️ تم تشغيله محلياً بس (سيرفر بناء إنتاج + قاعدة تطوير حقيقية)، **لم يُنفَّذ ضد رابط الإنتاج الحقيقي** في هذه الجلسة بسبب حجب الشبكة | راجع القسم 8 التاريخي |
| اختبار Android حقيقي | ❌ **NOT VERIFIED** (لا يوجد جهاز Android حقيقي في بيئة هذه الجلسة) — أي تحقق واجهة سابق كان **DESKTOP CHROMIUM VIEWPORT ONLY** عبر Playwright، مش جهاز حقيقي | راجع `DEVICE_QA_CHECKLIST.md` |

**ملاحظة صريحة عن اكتشاف حقيقي في هذه الجلسة**: أول push في هذه الجلسة (commit `456af89`)
كان فعلاً **فشل CI حقيقياً** — `GET /api/settings` كان بيرجع 500 على أي قاعدة بيانات جديدة
تماماً (fresh install)، لأن صف `store_settings` (id=1) كان بيتعمل بس من خلال سكربت `seed.ts`
الاختياري، اللي لا يعمل تلقائياً في CI ولا في مسار النشر الفعلي على Railway. تم اكتشاف
السبب بتشغيل مجموعة الاختبارات فعلياً ضد قاعدة بيانات Postgres جديدة تماماً (مش قاعدة
التطوير القديمة اللي كانت بتخفي المشكلة)، وتم إصلاحه بترحيلة (migration) جديدة تضمن وجود
الصف دايماً. بعد الإصلاح (commit `7915495`): CI ناجح، النشر ناجح، 1104/1104 اختبار ناجح
على قاعدتي بيانات جديدتين مستقلتين. هذا مثال فعلي على السبب اللي خلّى فحص "قاعدة بيانات
جديدة تماماً" (لا مجرد قاعدة التطوير المُرقّاة) بند إلزامي، مش اختياري.

## ملاحظات تحقّق تاريخية (HISTORICAL VERIFICATION NOTES)

الأقسام من هنا فصاعداً منقولة كما هي من نسخ سابقة من هذا الملف، كل قسم بتاريخه الأصلي —
لغرض التتبّع التاريخي فقط. **الحالة المعتمَدة حالياً هي القسم 0 فوق فقط.**

### 1. مشروعا Railway الموجودان — أيهما الإنتاج الفعلي (٢٠٢٦-٠٩-١٥)

في حساب Railway المرتبط فيه **مشروعان منفصلان**، كلاهما فيه خدمة اسمها `Alaa-eldin-`
متصلة بنفس مستودع GitHub وفرع `main`:

| المشروع | الخدمة | قاعدة بيانات مرفقة؟ | آخر نشر | الحالة |
|---|---|---|---|---|
| `endearing-clarity` (`bb9f5517-...`) | `Alaa-eldin-` (`bc329c97-...`) | ✅ خدمة `Postgres` منفصلة في نفس المشروع | ينشر تلقائياً مع كل push على `main`، آخر نشر فعلي بحالة `SUCCESS` | **هذا هو الإنتاج الفعلي — اُستخدم للتحقق الحي طوال هذه الجلسة** |
| `romantic-blessing` (`db3ba61c-...`) | `Alaa-eldin-` (`ed5b4846-...`) | ❌ لا توجد | `latestDeployment: null` — لم يُنشر عليها أي شيء فعلياً، `variableNames: []` (مفيش أي متغير بيئة متظبط) | **مشروع يتيم/تجريبي قديم — مش الإنتاج، ومفيهوش حتى DATABASE_URL**، فأي محاولة تشغيل عليه هتفشل فوراً بفضل الفحص في `server/src/db.ts` |

**تنبيه صريح**: لم يتم حذف `romantic-blessing` في هذه الجلسة ولا في أي جلسة سابقة، ولن يُحذف
تلقائياً — حذف مشروع/خدمة على Railway إجراء غير قابل للتراجع بيأثر على بنية تحتية فعلية، وده
قرار لازم يتاخد يدوياً من صاحب الحساب لو حابب يشيله، مش من الذكاء الاصطناعي من تلقاء نفسه.
لو حبيت تشيله فعلاً، تأكد الأول إنه مش مربوط بأي دومين حقيقي مستخدم حالياً.

### 2. متغيرات البيئة — المطلوب فعلياً مقابل الاختياري (٢٠٢٦-٠٩-١٥، مُحدَّث ٢٠٢٦-٠٩-٢٤)

`server/src/db.ts` بيفشل فوراً (`process.exit(1)`) لو `NODE_ENV=production` وممفيش
`DATABASE_URL` — الوحيد اللي فحصه كان صارماً من قبل هذه الدفعة. الدفعة دي أضافت
`server/src/config.ts`: فحص مركزي بيتنفذ مرة واحدة وقت الإقلاع (بعد التأكد من الاتصال بقاعدة
البيانات مباشرة) ويطبع سطر لوج واحد واضح (`startup_config_summary`) يوضّح أي تكامل مُفعّل
فعلياً وأيهم لأ — من غير ما يوقف السيرفر أبداً (كل التكاملات دي اختيارية بتصميم الميزة نفسها،
والتطبيق بالكامل بيشتغل من غيرها بس بميزات أقل). لو تكامل معين متظبط جزئياً بس (متغير موجود
والباقي ناقص)، بيتسجّل تحذير صريح (`startup_config_partial_warning`) بأسماء المتغيرات الناقصة
تحديداً — الحالة دي غالباً غلط كتابة في Railway مش قرار متعمّد.

| المتغير | مطلوب دائماً؟ | الحالة الفعلية في `endearing-clarity` (الإنتاج) الآن |
|---|---|---|
| `DATABASE_URL` | ✅ **إجباري** — السيرفر يرفض يقلع من غيره في الإنتاج | ✅ متظبط (خدمة Postgres المرفقة) |
| `PUBLIC_APP_URL` | مُستحسن (روابط استعادة كلمة المرور وغيرها) | ✅ متظبط |
| `RESEND_API_KEY` / `RESEND_FROM` | اختياري — بريد استعادة كلمة المرور فقط | ✅ `RESEND_API_KEY` متظبط، `RESEND_FROM` غير متظبط (بيرجع للقيمة الافتراضية `onboarding@resend.dev` — يفضل تغييرها لدومين حقيقي مُتحقق منه في Resend قبل إطلاق فعلي) |
| `CLOUDINARY_CLOUD_NAME` / `_API_KEY` / `_API_SECRET` | اختياري — رفع صور المنتجات | ✅ **متظبط الآن** (لوحظ لأول مرة ٢٠٢٦-٠٩-١٦ — اتضاف مباشرة عبر Railway → Variables بين جلسة الأمس وهذه الجلسة، مش بواسطة الذكاء الاصطناعي) — رفع صور المنتجات شغال فعلياً في الإنتاج |
| `WHATSAPP_ACCESS_TOKEN` / `_PHONE_NUMBER_ID` | اختياري — إشعارات WhatsApp الحقيقية | ✅ **متظبط الآن (٢٠٢٦-٠٩-٢٤)** — اتضاف مباشرة عبر Railway → Variables بمعرفة صاحب المشروع (توكن وحساب Meta حقيقيين، مش من الذكاء الاصطناعي) |
| `WHATSAPP_ORDER_CONFIRMATION_TEMPLATE` (+ `_LANGUAGE` اختياري) | اختياري — اسم قالب تأكيد الطلب المعتمد فعلياً من Meta Business Manager؛ بدونه تأكيد الطلب التلقائي عبر واتساب مش هيتبعت حتى لو التوكن ورقم الهاتف فوق مضبوطين | ✅ **متظبط الآن (٢٠٢٦-٠٩-٢٤)** — القالب (`order_confirmation_ar`) تم اعتماده فعلياً في Meta Business Manager بمعرفة صاحب المشروع؛ تم التحقق فعلياً من لوج الإقلاع بعد إعادة النشر (`startup_config_summary` بيعرض `whatsapp_order_confirmation` جوه `configured`، من غير أي `startup_config_partial_warning`) |
| `VAPID_PUBLIC_KEY` / `_PRIVATE_KEY` / `_SUBJECT` | اختياري — إشعارات المتصفح (Web Push) | ✅ **متظبط الآن (٢٠٢٦-٠٩-٢٤)** — زوج مفاتيح VAPID اتولّد محلياً (`npx web-push generate-vapid-keys`، ما فيهوش أي حساب خارجي مطلوب) واتضاف في Railway → Variables بمعرفة صاحب المشروع بعد موافقته. تم التحقق فعلياً من لوج الإقلاع بعد إعادة النشر — `startup_config_summary` بيعرض `web_push` جوه `configured` من غير أي تحذير جزئي |
| `TURNSTILE_SITE_KEY` / `TURNSTILE_SECRET_KEY` | اختياري — CAPTCHA (Cloudflare Turnstile) على التسجيل/نسيت كلمة المرور/تسجيل الدخول التكيّفي | ✅ **متظبط الآن** — كان "غير متظبط" وقت فحص القسم 8 (٢٠٢٦-٠٩-٢١)، لكن ظهر متضبطاً في فحص Railway API بتاريخ ٢٠٢٦-٠٩-٢٤ (نفس نمط Cloudinary — اتضاف مباشرة بمعرفة صاحب المشروع بين الجلستين، مش بواسطة الذكاء الاصطناعي) |
| `SESSION_IP_HASH_SALT` | اختياري لكن مُستحسن بشدة | ✅ **متظبط الآن** (٢٠٢٦-٠٩-١٦) — قيمة عشوائية ثابتة (64 حرف hex، مولّدة بـ `crypto.randomBytes(32)`) اتضبطت مباشرة عبر Railway → Variables، فأي IP hash مخزّن هيفضل يتقارن صح عبر عمليات إعادة التشغيل/النشر الجاية. القيمة نفسها لم تُسجَّل في أي مكان (مش في git، مش في اللوج) — لو احتجت تغييرها، اضبطها من Railway → Variables مباشرة |

**خلاصة عملية (مُحدَّثة ٢٠٢٦-٠٩-٢٤)**: الإنتاج الحالي شغال وآمن، وكل التكاملات
الاختيارية الموثّقة في الجدول فوق بقت مفعّلة فعلياً — Cloudinary، WhatsApp (تأكيد
الطلب التلقائي بقالب Meta معتمد)، Web Push (VAPID)، وTurnstile. تم التحقق المباشر من
كل واحدة عبر لوج الإقلاع الفعلي بعد كل نشر (`startup_config_summary`)، مش بس عبر أسماء
المتغيرات. آخر تحقق مباشر: `configured: ["cloudinary","whatsapp","whatsapp_order_confirmation","web_push","email","turnstile"]`،
`notConfigured: []` — مفيش أي تكامل اختياري ناقص حالياً.

### 3. نقطة `/health` (٢٠٢٦-٠٩-١٥)

`GET /health` بترجع بس `{ status: 'ok' | 'error' }` بناءً على `SELECT 1` حقيقي على قاعدة
البيانات — من غير مصادقة ومن غير أي تفاصيل داخلية أو أسرار. دي المستخدمة فعلياً كـ healthcheck
لـ Railway. تم مراجعتها في هذه الدفعة والتأكد إنها لا تسرّب أي معلومة حساسة.

### 4. التكامل المستمر (`.github/workflows/ci.yml`) (٢٠٢٦-٠٩-١٥)

تمت مراجعته في هذه الدفعة — يشغّل فعلياً: تثبيت الاعتماديات الثلاثة، تطبيق الترحيلات على
Postgres حقيقي (خدمة `postgres:16` في نفس الـ workflow)، تشغيل مجموعة اختبارات السيرفر
الكاملة، ثم بناء الثلاث تطبيقات (يشمل `tsc` الصارم في كل بناء، فأي خطأ نوع بيوقف الـ CI). لا
يوجد خطوة `lint` منفصلة لأن المشروع لا يحتوي على إعداد ESLint من الأساس — هذا خارج نطاق هذه
الدفعة (تغيير بنيوي، مش تنظيف/تحقق).

### 5. النسخ الاحتياطي والاستعادة (٢٠٢٦-٠٩-١٥)

راجع `server/docs/BACKUP_RESTORE.md` (موجود من دفعة سابقة) — لسه صالح وما اتغيّرش في هذه
الدفعة.

### 6. دفعة الولاء (استخدام النقاط + انتهاء الصلاحية) والإحالة — حالة فعلية (٢٠٢٦-٠٩-٢٠)

هذا القسم بيوضّح حالة الدفعة اللي أضافت استخدام نقاط الولاء عند الدفع، انتهاء صلاحية النقاط
(FIFO)، ومكافأة العميل الجديد المُحال + صفحة إدارة الإحالات — مكتوب من فحص فعلي وقت كتابة
هذا القسم، مش افتراضات.

| البند | الحالة الفعلية |
|---|---|
| استخدام النقاط عند الدفع (redemption) | ✅ **مكتمل ومنشور فعلياً** — حساب سيرفر بالكامل (الرصيد/الحد الأدنى/الأقصى/القيمة)، ذرّي مع إنشاء الطلب، محمي من الصرف المزدوج بقفل صفوف Postgres حقيقي، ومحترم لآلية idempotency الحالية. |
| انتهاء صلاحية النقاط | ✅ **الكود مكتمل ومنشور** — لكن **معطّل من إعدادات المتجر افتراضياً** (`loyalty_expiry_enabled = false`)، والمعالج (`npm run loyalty:expire`) **لا يعمل تلقائياً** لحد ما يتضبط له جدولة خارجية. راجع القسم أسفله. |
| مكافأة العميل الجديد المُحال | ✅ مكتمل ومنشور — معطّلة فعلياً بالقيمة الافتراضية (`referral_referred_bonus_points = 0`)؛ تحتاج ضبط قيمة أكبر من صفر من لوحة التحكم لتفعيلها. |
| صفحة إدارة الإحالات (`/admin/customers/referrals`) | ✅ مكتمل ومنشور — قائمة/فلترة/ترقيم/تفاصيل/تحليلات، محمي بصلاحية `referrals.view` (ممنوحة لـ `role-manager` بس حالياً). |
| **جدولة Railway Cron لمعالج انتهاء الصلاحية** | ❌ **REQUIRES PRODUCTION CRON CONFIGURATION** — لم يتم إنشاء أي Railway Cron Job فعلي. الأمر جاهز (`npm run loyalty:expire`) والتوثيق الكامل في `server/docs/LOYALTY_EXPIRY_CRON.md`، لكن لازم صاحب المشروع يضيف خدمة Cron يدوياً على Railway قبل ما ميزة انتهاء الصلاحية (لو اتفعّلت) تبقى فعّالة فعلياً في الإنتاج. |
| اختبارات السيرفر (كل المشروع، مش الدفعة دي بس) | ✅ **883/883 ناجحة** (كانت 799 قبل هذه الدفعة — 84 اختبار جديد مضاف: استخدام نقاط، انتهاء صلاحية، إحالات، تفاعل مع الخصومات/المتغيرات/الإلغاء، أمان/تخصيص جماعي/IDOR/تزامن حقيقي). |
| فحص أنواع TypeScript + بناء السيرفر/المتجر/لوحة التحكم | ✅ الثلاثة نظاف بدون أخطاء وقت كتابة هذا القسم. |
| GitHub CI (`main`, آخر commit) | ✅ **نجاح فعلي مؤكد** عبر GitHub API مباشرة (مش افتراض) — راجع سجل الـ commits. |
| نشر Railway (`endearing-clarity`/`Alaa-eldin-`) | ✅ **SUCCESS فعلياً** — تم التحقق من حالة الخدمة (`online`) ولوج النشر الحقيقي (تطبيق الترحيلين 0049/0050 بنجاح، `database_connected`, `server_started`) عبر Railway MCP مباشرة. |
| `/health` في الإنتاج | ⚠️ **لم يتحقق منه مباشرة بطلب HTTP خارجي** — بيئة هذه الجلسة تمنع أي اتصال خارج مباشر لدومين `alaa-eldin-production.up.railway.app` (سياسة عزل شبكة على مستوى الحساب). الدليل غير المباشر قوي مع ذلك: `healthcheckPath: /health` في `railway.json` هو نفسه اللي Railway بيعتمد عليه قبل ما يعلّم أي نشر كـ `SUCCESS`/`online`، وده حصل فعلاً لهذا النشر. **لم يتم تأكيد نص الرد الخام (`{"status":"ok"}`) من خارج شبكة Railway نفسها في هذه الجلسة.** |
| اختبار دخان إنتاجي حقيقي (حركة مرور خارجية فعلية) | ❌ **لم يُنفَّذ** لنفس سبب حجب الشبكة أعلاه — بديل ذلك: تشغيل السيرفر محلياً (نفس بناء الإنتاج، قاعدة بيانات تطوير حقيقية) والتحقق من الصفحات الجديدة فعلياً عبر Playwright (راجع `DEVICE_QA_CHECKLIST.md`). |
| اختبار على جهاز Android حقيقي | ❌ لم يُنفَّذ (بيئة هذه الجلسة بدون جهاز حقيقي) — راجع `DEVICE_QA_CHECKLIST.md`. |

### 7. قبل أي إطلاق فعلي لمستخدمين حقيقيين (٢٠٢٦-٠٩-٢٠)

- [ ] تأكد إن الدومين الفعلي (لو فيه) متظبط على خدمة `endearing-clarity`/`Alaa-eldin-` تحديداً، مش `romantic-blessing`.
- [ ] راجع القسم 2 فوق وقرر أي التكاملات الاختيارية عايز تفعّلها فعلياً قبل الإطلاق.
- [x] Cloudinary (رفع صور المنتجات) — متظبط ومؤكد في لوج `startup_config_summary` الحقيقي.
- [x] WhatsApp (تأكيد الطلب التلقائي + قالب Meta معتمد)، Web Push (VAPID)، Turnstile — الثلاثة متظبطين الآن (٢٠٢٦-٠٩-٢٤) ومؤكدين في لوج `startup_config_summary` الحقيقي بعد إعادة النشر — راجع القسم 2 فوق.
- [ ] نفّذ نسخة احتياطية يدوية واحدة على الأقل (القسم 5) قبل أي إطلاق فعلي.
- [ ] راجع `DEVICE_QA_CHECKLIST.md` فعلياً على جهاز Android حقيقي — لم يُنفَّذ أي بند منه في أي جلسة AI سابقة.
- [ ] شغّل `npm run smoke-test --prefix server` (مع `SMOKE_TEST_URL` مضبوط على رابط الإنتاج الفعلي، وأيضاً `SMOKE_TEST_CUSTOMER_EMAIL/PASSWORD` و`SMOKE_TEST_ADMIN_EMAIL/PASSWORD` لو عايز الفحوصات المصادَق عليها) بعد أول نشر حقيقي.
- [ ] لو حابب تفعّل انتهاء صلاحية نقاط الولاء (`loyalty_expiry_enabled`)، **لازم** تضبط Railway Cron Job فعلياً أولاً (راجع `server/docs/LOYALTY_EXPIRY_CRON.md`) — تفعيل الإعداد بدون الجدولة يعني نقاط لن تنتهي أبداً فعلياً رغم ظهور الإعداد "مفعّل".
- [ ] كذلك تذكير السلة المهجورة يحتاج Railway Cron Job منفصل (راجع `server/docs/ABANDONED_CART_CRON.md`) قبل ما يشتغل تلقائياً في الإنتاج.
- [ ] قرر قيمة `referral_referred_bonus_points` (حالياً 0 — معطّلة) لو عايز تكافئ العميل الجديد المُحال مش المُحيل بس.
- [ ] تحقق من `GET /health` ورحلة تسجيل دخول/تصفّح كاملة على الرابط الحقيقي مباشرة بعد أي نشر (لم يتحقق منها AI في هذه الجلسة بسبب عزل الشبكة — راجع القسم 8).

### 8. دفعة CAPTCHA/Cron/Smoke Test/جاهزية الإطلاق — حالة فعلية (٢٠٢٦-٠٩-٢١)

هذا القسم بيوضّح حالة دفعة Cloudflare Turnstile (CAPTCHA)، مراجعة جاهزية Railway Cron
(انتهاء صلاحية الولاء + تذكير السلة المهجورة)، توسيع اختبار الدخان الإنتاجي، وفحوصات
جاهزية الإطلاق النهائية — مكتوب من فحص فعلي وقت كتابة هذا القسم، مش افتراضات.

| البند | الحالة الفعلية |
|---|---|
| Cloudflare Turnstile (CAPTCHA) | ✅ **الكود جاهز بالكامل ومنشور** (خدمة تحقق مركزية، ربط بالتسجيل/نسيت كلمة المرور/تسجيل الدخول التكيّفي للعميل والإدارة، fail-closed عند تعطّل المزوّد، widget فعلي في الواجهتين). كان ❌ **غير مُفعّل في الإنتاج** وقت كتابة هذا الصف (٢٠٢٦-٠٩-٢١) — `TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY` غير موجودين ضمن متغيرات Railway وقتها. **تحديث لاحق (٢٠٢٦-٠٩-٢٤)**: بقى ✅ متظبط فعلياً — راجع القسم 2 فوق. |
| سياسة CAPTCHA التكيّفية | العميل العادي: بعد 3 محاولات دخول فاشلة على نفس الإيميل. الإدارة: بعد محاولة واحدة فقط (قرار أمني موثّق — حساب إداري أخطر). التتبّع في جدول `login_failure_tracking` (قاعدة بيانات، مش ذاكرة السيرفر) — يفضل صحيح عبر إعادة التشغيل. |
| اختبارات CAPTCHA | ✅ **24 اختبار جديد** (`turnstileService.test.ts`، `captchaRoutes.test.ts`) — تغطي: توكن ناقص، تحقق ناجح، توكن غير صالح/منتهي/مُعاد استخدامه، تايم آوت المزوّد، خطأ شبكة، رد غير مفهوم، حماية التسجيل، حماية نسيت كلمة المرور (مع الحفاظ على عدم كشف وجود الحساب)، الانتقال التكيّفي، سلوك دخول الإدارة، استمرار عمل الـ rate limiter، عدم رجوع السر أبداً. Cloudflare نفسها مُقلَّدة بالكامل — لا يوجد أي اتصال حقيقي بالمزوّد في أي اختبار. |
| جدولة Railway Cron لمعالج انتهاء صلاحية الولاء | ❌ **REQUIRES PRODUCTION CRON CONFIGURATION** (بدون تغيير عن القسم 6) — تمت إضافة توصية دقيقة لجدولة UTC مع توضيح صريح لعدم اليقين الدائم بسبب تغييرات مصر المتكررة في سياسة التوقيت الصيفي. راجع `server/docs/LOYALTY_EXPIRY_CRON.md` المُحدَّث. |
| جدولة Railway Cron لتذكير السلة المهجورة | ❌ **REQUIRES PRODUCTION CRON CONFIGURATION** — لم تكن موثّقة بملف مستقل قبل هذه الدفعة. تم إنشاء `server/docs/ABANDONED_CART_CRON.md` بالكامل، بالإضافة لاكتشاف وإصلاح ثغرة تكرار تحت التداخل (راجع البند التالي). |
| أمان تحت التداخل (Overlapping Cron Runs) | ⚠️ **تم اكتشاف وإصلاح ثغرة حقيقية** في `sendAbandonedCartReminders` — النسخة الأصلية كانت ممكن تبعت تذكير مكرر لنفس العميل لو تشغيلتين اشتغلوا في نفس الوقت بالخطأ. تم إصلاحها بحجز ذرّي على مستوى قاعدة البيانات (`UPDATE ... WHERE reminder_sent_at IS NULL`)، مع اختبار تلقائي صريح يشغّل تشغيلتين متزامنتين فعلياً ويتأكد من إرسال إشعار واحد بالظبط. معالج انتهاء صلاحية الولاء كان آمن أصلاً من قبل (`FOR UPDATE SKIP LOCKED`). |
| اختبار الدخان (`smoke-test`) | ✅ **تم توسيعه** (نفس البنية الموجودة، مش نسخة جديدة) — أضاف: الصفحة الرئيسية، صفحة دخول الإدارة، رفض API الإدارة بدون مصادقة، هيدرز الأمان (Helmet)، فحص HTTPS للرابط، مسارات حساسة (`.env`/`.git`/`server`/`backup`/`logs`) لازم ترجع 404، وفحوصات مصادَق عليها اختيارية (عميل + إدارة) لو `SMOKE_TEST_CUSTOMER_EMAIL/PASSWORD` و`SMOKE_TEST_ADMIN_EMAIL/PASSWORD` متضبوطين. تم تشغيله فعلياً محلياً (سيرفر بناء إنتاج حقيقي + قاعدة بيانات تطوير) وتأكيد نجاح 16/17 بند (البند الوحيد اللي فشل هو فحص HTTPS، وده متوقع تماماً لأن الاختبار المحلي كان على `http://localhost`). |
| اختبارات السيرفر (كل المشروع) | ✅ **908/908 ناجحة** (كانت 883 قبل هذه الدفعة — 25 اختبار جديد: 24 لـ CAPTCHA + اختبار واحد لأمان التداخل في السلة المهجورة). |
| فحص أنواع TypeScript + بناء السيرفر/المتجر/لوحة التحكم | ✅ الثلاثة نظاف بدون أخطاء وقت كتابة هذا القسم. |
| GitHub CI (`main`, آخر commit مدفوع فعلياً) | ✅ **نجاح فعلي مؤكد** عبر GitHub API مباشرة — آخر commit مدفوع (`3372d0b`) حالته `completed`/`success`. **تنبيه**: التغييرات في هذه الدفعة (Turnstile/Cron docs/smoke test) لسه ما اتدفعتش لـ GitHub وقت كتابة هذا السطر — الحالة دي تعكس آخر push حقيقي، مش الكود المحلي غير المدفوع. |
| نشر Railway (`endearing-clarity`/`Alaa-eldin-`) | ✅ **SUCCESS فعلياً** لآخر نشر (يطابق نفس commit `3372d0b` أعلاه) — تم التحقق عبر Railway API مباشرة (`latestDeployment.status: "SUCCESS"`). لا توجد أي خدمة Cron Job على المشروع (تم التأكد عبر `list-services` — خدمتان بس: `Postgres` و`Alaa-eldin-`). |
| `/health` في الإنتاج | ⚠️ **لم يتحقق منه مباشرة بطلب HTTP خارجي** في هذه الجلسة (تم تجربة `curl` و`WebFetch` كلاهما — نفس حجب الشبكة `EGRESS_BLOCKED`/`connect_rejected`). الدليل غير المباشر: `healthcheckPath: /health` في `railway.json` + Railway نفسها بتعتمد عليه قبل ما تعلّم أي نشر `SUCCESS`، وده حصل فعلاً. |
| HTTPS/دومين الإنتاج | الدومين `alaa-eldin-production.up.railway.app` مُدار بالكامل من Railway (`railwayManaged: true`) — دومينات Railway المُدارة زي دي بتتقدم دايماً عبر HTTPS بشهادة تلقائية من المنصة، لكن **لم يتم تأكيد هيدر HSTS الفعلي ولا صحة الشهادة مباشرة** من هذه الجلسة بسبب حجب الشبكة نفسه. |
| WhatsApp | ❌ **غير متظبط** وقت كتابة هذا الصف (٢٠٢٦-٠٩-٢٢) — تم التأكد فعلياً عبر Railway API إن المتغيرات غير موجودة. شاشة التكاملات بالفعل بتعرض "غير مفعّل" بشكل صحيح (كود موجود من دفعة سابقة، لم يتغيّر). **تحديث لاحق (٢٠٢٦-٠٩-٢٤)**: بقى ✅ متظبط فعلياً — راجع القسم 2 فوق (`WHATSAPP_ACCESS_TOKEN`/`_PHONE_NUMBER_ID`/`WHATSAPP_ORDER_CONFIRMATION_TEMPLATE`) للتفاصيل والتحقق الفعلي من لوج الإقلاع. |
| Web Push (VAPID) | ❌ **غير متظبط** وقت كتابة هذا الصف (٢٠٢٦-٠٩-٢٢) — نفس التأكيد. **تحديث لاحق (٢٠٢٦-٠٩-٢٤)**: بقى ✅ متظبط فعلياً — زوج مفاتيح VAPID اتولّد وتم إضافته لـ Railway، راجع القسم 2 فوق للتفاصيل والتحقق الفعلي من لوج الإقلاع. |
| اختبار على جهاز Android حقيقي | ❌ لم يُنفَّذ (بيئة هذه الجلسة بدون جهاز حقيقي) — تم تحديث `DEVICE_QA_CHECKLIST.md` بأقسام جديدة كاملة لـ CAPTCHA/واتساب/Push/لوحة التحكم/واجهة المتجر/النسخ الاحتياطي. |
| نسخة احتياطية للإنتاج | ❌ **NOT VERIFIED** — هذه الجلسة لا تملك بيانات اعتماد وصول مباشر لقاعدة بيانات الإنتاج الحقيقية على Railway (`list-variables` بيرجع القيم مُخفاة/`valuesRedacted: true`)، فتنفيذ نسخة احتياطية فعلية للإنتاج من هنا غير ممكن تقنياً. لازم تُنفَّذ يدوياً بمعرفة صاحب الوصول. |

### 9. دفعة الباركود/البحث الشامل/تذاكر الدعم — حالة فعلية (٢٠٢٦-٠٩-٢٢)

هذا القسم بيوضّح حالة دفعة طباعة ملصقات الباركود، البحث الإداري الشامل، وتذاكر دعم
العملاء — مكتوب من فحص فعلي لأحدث `main` (`3790b2b`) وقت كتابة هذا القسم، مش افتراضات.

| البند | الحالة الفعلية |
|---|---|
| طباعة ملصقات الباركود/السعر | ✅ مكتمل ومنشور — توليد/تحقق باركود، صفحة بحث للطباعة، بناء ملصق قابل للطباعة في لوحة التحكم. |
| البحث الإداري الشامل | ✅ مكتمل ومنشور — فهرسة `pg_trgm`/GIN، تطبيع عربي مشترك، واجهة command palette. |
| تذاكر دعم العملاء | ✅ مكتمل ومنشور — تم التحقق مباشرة في هذه الجلسة من كود المسارات واختبارات HTTP الحقيقية (`support.ts`, `adminSupport.test.ts`, `support.test.ts`): عزل التذاكر بين العملاء (404 مش تسريب)، الملاحظات الداخلية لا تظهر أبداً عبر واجهة العميل، RBAC دقيق (view/reply/assign/manage منفصلين وتم اختبارهم)، تسجيل audit log عند تغيير الحالة/الأولوية/التعيين، ربط الطلب يتحقق من الملكية فعلياً (لا يقبل رقم طلب عميل آخر). |
| اختبارات السيرفر (كل المشروع) | ✅ **990/990 ناجحة**، 80/80 ملف — تم التحقق بتشغيل المجموعة الكاملة مرتين متتاليتين في هذه الجلسة (فشل عابر أول مرة كان تلوث بيانات اختبار من ملف تاني، مش خلل حقيقي بالكود). |
| الترحيلات (migrations) | 54 ملف، آخرها `0054_support_tickets.sql`. |
| GitHub CI (`main`, آخر commit) | ✅ نجاح فعلي مؤكد لـ `3790b2b` عبر GitHub API مباشرة. |
| نشر Railway (`endearing-clarity`/`Alaa-eldin-`) | ✅ SUCCESS فعلياً، يطابق نفس commit `3790b2b`. |
| PRs مفتوحة (Dependabot ×6 + PR #2) | راجع سجل مراجعة الـ PRs المنفصل — PR #2 (auto-slug + optional barcode) **مُستبدَل بالكامل بكود موجود فعلياً في `main` الحالي** (تم التأكد بقراءة `productService.ts` و`ProductFormPage.tsx` مباشرة) — أُغلق كمُستبدَل، لم يُدمج. |
