// تطبيع نص عربي للمقارنة وقت البحث فقط — الاسم المخزّن في قاعدة البيانات (وعلى الواجهة) ما
// بيتغيرش خالص. نفس الاسم ممكن يتكتب بأكتر من شكل شائع (أ/إ/آ كلها همزة على الألف، ى بدل ي
// في آخر الكلمة، ة بدل ه في العامية). تعريف واحد مُصدَّر يُستخدم في كل مكان محتاج بحث عربي
// متسامح (بحث الكتالوج، البحث الشامل في لوحة التحكم) — بدون تكرار نفس المنطق أكتر من مرة.
export const NORMALIZE_SQL = (expr: string) => `lower(translate(${expr}, 'أإآىة', 'ااايه'))`
