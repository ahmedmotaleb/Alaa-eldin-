# قائمة فحص الأجهزة الحقيقية (Android Chrome)

هذا الملف قائمة فحص لإنسان يشغّل الموقع فعلياً على جهاز Android حقيقي بمتصفح Chrome —
**لم يتم تنفيذ أي بند هنا فعلياً بواسطة الذكاء الاصطناعي في هذه الجلسة**، لأنه لا يملك وصولاً
لجهاز Android فعلي أو محاكي حقيقي. كل ما تم التحقق منه فعلياً في هذه الجلسة هو:
- بناء التطبيقات الثلاثة (`npm run build`) بنجاح.
- تشغيل مجموعة اختبارات السيرفر الكاملة (Vitest) بنجاح.
- تحقّق حي عبر Playwright (متصفح Chromium على خادم Linux، مش جهاز Android فعلي) لصفحات
  مثل الإيصال (Phase 100)، ولاحقاً صفحات نقاط الولاء/الإحالة الجديدة (راجع القسم 7)،
  والتحقق من استجابة الـ API مباشرة عبر curl.

استخدم القائمة دي كخطوات يدوية حقيقية قبل أي إطلاق إنتاجي فعلي على مستخدمين حقيقيين.

## 1. تركيب PWA على الشاشة الرئيسية
- [ ] افتح رابط المتجر الحقيقي في Chrome على Android.
- [ ] تأكد إن Chrome بيعرض خيار "إضافة إلى الشاشة الرئيسية" / "تثبيت التطبيق" (مش لقطة شاشة عادية).
- [ ] بعد التثبيت، افتح الأيقونة من الشاشة الرئيسية وتأكد إنها بتفتح في وضع standalone (بدون شريط عنوان المتصفح).
- [ ] كرّر نفس الخطوات للوحة التحكم (`/admin`) بشكل منفصل — تأكد إن أيقونتها مختلفة عن المتجر ومفيهاش تعارض بينهم.

## 2. كاميرا مسح الباركود (لوحة التحكم → المخزون → مسح الباركود)
- [ ] **الحالة الطبيعية (ready)**: افتح الصفحة أول مرة، تأكد إن زرار "ابدأ المسح بالكاميرا" ظاهر ومفيش أي طلب إذن تلقائي عند فتح الصفحة (الإذن لازم يتطلب بس بعد الضغط على الزرار).
- [ ] اضغط الزرار، وافق على إذن الكاميرا، وتأكد إن معاينة الفيديو بتظهر وإن مسح باركود حقيقي (على منتج أو عبوة فعلية) بيتعرّف عليه ويملى حقل الباركود تلقائياً.
- [ ] **حالة رفض الإذن (permission-denied)**: افتح الصفحة في وضع خاص (Incognito) جديد، اضغط "ابدأ المسح"، وارفض إذن الكاميرا صراحة. تأكد إن الرسالة بتوضّح إن الإذن اتُرفض وإزاي يتفعّل يدوياً من إعدادات الموقع، ومفيش أي طلب إذن تاني بيظهر تلقائياً.
- [ ] **حالة عدم توفر كاميرا (unavailable)**: لو متاح، جرّب على جهاز/متصفح من غير كاميرا فعلياً وتأكد من ظهور رسالة واضحة بدل خطأ عام.
- [ ] **حالة عدم الدعم (unsupported)**: افتح نفس الصفحة على متصفح مايدعمش `BarcodeDetector` (Safari على iOS مثلاً) وتأكد إن الرسالة بتوضّح كده، وإن حقل الإدخال اليدوي لسه شغال بشكل طبيعي.
- [ ] **قارئ باركود فيزيائي (لو متاح)**: وصّل قارئ USB/بلوتوث، تأكد إنه بيتصرف كلوحة مفاتيح عادية ويملى الحقل ويبحث تلقائياً بعد Enter.

## 3. إشعارات المتصفح (Web Push) — الواجهة (حسابي → البيانات الشخصية)
- [ ] **الحالة الطبيعية (not_subscribed → subscribed)**: اضغط "تفعيل الإشعارات"، وافق على الإذن، تأكد من ظهور رسالة "الإشعارات مفعّلة على هذا الجهاز"، وإن تحديث حالة طلب حقيقي بيوصل كإشعار فعلي للجهاز.
- [ ] **حالة رفض الإذن (denied)**: ارفض الإذن مرة، أعد فتح الصفحة، وتأكد إن الزرار اختفى وظهرت رسالة "تم رفض الإذن — فعّله من إعدادات المتصفح" بدل زرار هيفشل، ومفيش أي طلب إذن جديد بيظهر تلقائياً.
- [ ] **حالة عدم الدعم (unsupported)**: افتح على متصفح مايدعمش Push API وتأكد من ظهور الرسالة المناسبة.
- [ ] **حالة عدم التفعيل من السيرفر (server not configured)**: لو مفاتيح VAPID مش متظبطة على السيرفر، تأكد إن الرسالة بتوضّح إن الخدمة مش مفعّلة من المتجر، بدل زرار قابل للضغط هيفشل.
- [ ] **اشتراك قديم (stale subscription)**: فعّل الإشعارات، بعدين احذف صف الاشتراك يدوياً من قاعدة البيانات (محاكاة لفشل إرسال سابق مسح الصف من السيرفر)، أعد فتح الصفحة وتأكد إن الحالة بترجع تتزامن تلقائياً مع السيرفر من غير أي طلب إذن جديد.

## 4. الطباعة والمشاركة (إيصال الطلب)
- [ ] افتح إيصال طلب حقيقي على الموبايل، اضغط "مشاركة" وتأكد إن نافذة المشاركة القياسية لـ Android بتفتح (أو إن الرابط بينسخ للحافظة كبديل لو المشاركة مش مدعومة).
- [ ] اضغط "طباعة" وتأكد إن معاينة الطباعة بتظهر بشكل نظيف (من غير الهيدر أو الأزرار)، حتى لو مفيش طابعة فعلية متاحة (تأكيد المعاينة كافٍ).

## 5. الشاشات الأساسية (mobile-first)
- [ ] الصفحة الرئيسية، السلة، الدفع، وصفحة الإيصال على عرض شاشة حقيقي (Android عادي ≈ 360-412px) — تأكد من عدم وجود تمرير أفقي أو نصوص متقطعة.
- [ ] تأكد من ظهور الأرقام بشكل غربي (0-9) في كل مكان (الأسعار، الكميات، التواريخ) — مفيش أي رقم هندي/شرقي ظاهر بالغلط.

## 6. لوحة تحكم الإدارة — إصلاح التخطيط المتجاوب على الموبايل

**ما تم التحقق منه فعلياً في هذه الجلسة (وما لم يتم):** التحقق تم عبر Chromium على خادم
Linux مع محاكاة أبعاد الشاشة (`viewport emulation` بمكتبة Playwright) — **ده مش جهاز
Android حقيقي ولا محاكي Android حقيقي** (مفيش نظام Android، مفيش متصفح Chrome for
Android الفعلي، مفيش لمس حقيقي touch events، ومحرك الريندرة نفسه هو Chromium ديسكتوب
بس بعرض شاشة مصغّر). النتائج دي **تحقق محلي بمحاكاة أبعاد فقط**، مش تحقق على جهاز/محاكي
Android فعلي — لازم يتعمل الفحص الحقيقي التالي يدوياً على جهاز فعلي قبل أي إطلاق:

- [ ] افتح `/admin` من متصفح Chrome حقيقي على جهاز Android فعلي (مش محاكاة).
- [ ] تأكد إن الشريط الجانبي مقفول افتراضياً، وزرار القائمة (☰) ظاهر في الهيدر.
- [ ] اضغط زرار القائمة، وتأكد إن الشريط بيفتح من ناحية اليمين بحركة سلسة، ومعاه خلفية
      داكنة نصف شفافة فوق باقي الصفحة.
- [ ] اضغط على الخلفية الداكنة (برة الشريط) وتأكد إن الشريط بيتقفل.
- [ ] افتح الشريط تاني واختار أي عنصر تنقل (زي "الطلبات") وتأكد إن الشريط بيتقفل تلقائياً
      وإن الصفحة الجديدة بتفتح صح.
- [ ] افتح "إضافة منتج" وتأكد إن كل الحقول (الاسم، الوصف، الأقسام، السعر، السعر قبل
      الخصم، التكلفة، الوحدة، المخزون...) ظاهرة كاملة وقابلة للضغط من غير أي تمرير أفقي
      للصفحة، وإن حقول السعر التلاتة (البيع/قبل الخصم/التكلفة) نازلة تحت بعض مش متزاحمة.
- [ ] افتح "مركز التنبيهات" وتأكد إن كل صف تنبيه ظاهر كامل من غير قطع أو تمرير أفقي.
- [ ] افتح أي صفحة فيها جدول بيانات كبير (زي "جميع الطلبات") وتأكد إن الجدول نفسه بس
      اللي بيتمرر أفقياً لو احتاج (بإصبعك جوه الجدول)، مش الصفحة كلها.
- [ ] كرّر نفس الفحوصات على جهاز Android بشاشة أعرض شوية (تابلت صغير أو هاتف كبير).

**ملخص التحقق الآلي/المحلي اللي تم فعلاً** (Chromium + محاكاة أبعاد، وليس جهاز حقيقي):
تم تسجيل دخول حقيقي وتصفح Dashboard/إضافة منتج/مركز التنبيهات/الطلبات على أبعاد
360×800، 390×844، 412×915، 768×1024 (موبايل/تابلت) و1280×800 (ديسكتوب)، مع التحقق
برمجياً من عدم وجود `overflow` أفقي على مستوى الصفحة (`document.documentElement.scrollWidth`)
في كل الحالات، ومن سلوك الـ drawer (فتح/قفل بالزرار، ظهور الخلفية، القفل بالضغط على
الخلفية، القفل عند اختيار عنصر تنقل) في كل الأبعاد الموبايل/تابلت، بالإضافة لمراجعة
بصرية فعلية للقطات الشاشة الناتجة.

## 7. نقاط الولاء واستخدامها + الإحالات — فحوصات جديدة (٢٠٢٦-٠٩-٢٠)

**ما تم التحقق منه فعلياً في هذه الجلسة (وما لم يتم):** نفس منهجية القسم 6 بالظبط —
Chromium على خادم Linux بمحاكاة أبعاد الشاشة، جلسة دخول حقيقية (كوكي جلسة حقيقي مُصدَر من
نفس منطق تسجيل الدخول، مش mock)، وسيرفر مبني بوضع الإنتاج فعلياً (`NODE_ENV=production`)
لكن **بقاعدة بيانات تطوير محلية، مش قاعدة بيانات/رابط الإنتاج الحقيقي على Railway** — تم
التحقق من الصفحات التالية بدون أي `overflow` أفقي أو أرقام هندية/شرقية ظاهرة على الأبعاد
360×740، 390×844، 412×915، 768×1024، و1280×800:
- استخدام النقاط عند الدفع (قسم "رصيد النقاط" في صفحة إتمام الطلب، شامل زرار "استخدام
  الحد الأقصى" وحقل الإدخال اليدوي).
- صفحة "المكافآت" (RewardsPage) — الرصيد، قيمته بالجنيه، قواعد الاستخدام، الإحالة.
- إعدادات المتجر → قسم برنامج الولاء/الإحالة الجديد في لوحة التحكم.
- صفحة "العملاء → الإحالات" (القائمة والتفاصيل) في لوحة التحكم.
- ملف العميل في لوحة التحكم (رصيد النقاط، سجل الحركات، نموذج التعديل اليدوي).
- درج تفاصيل الطلب في لوحة التحكم (قسم نقاط الولاء الجديد فيه).
- الإيصال (خصم النقاط، عدد النقاط المستخدمة).

**لم يتم فعلياً ولازم يتعمل يدوياً على جهاز Android حقيقي قبل أي إطلاق:**
- [ ] نسخ كود الإحالة ومشاركته فعلياً عبر ورقة المشاركة الحقيقية لنظام Android (`navigator.share`) — التحقق الآلي أعلاه ما اختبرش ورقة المشاركة الحقيقية للنظام نفسها.
- [ ] استخدام نقاط فعلي في طلب حقيقي كامل (مش مجرد عرض الصفحة) من جهاز حقيقي، والتأكد إن الرصيد بيتحدّث صح بعد الطلب.
- [ ] رسالة "رصيد النقاط تغيّر" فعلياً (بفتح نفس الحساب من جهازين وقت واحد ومحاولة استخدام نفس النقاط) — سيناريو تزامن حقيقي، اتغطى آلياً في اختبارات السيرفر (Vitest) لكن مش على واجهة حقيقية.
- [ ] "نقاط ستنتهي قريباً" على جهاز حقيقي — يحتاج تفعيل انتهاء الصلاحية من إعدادات المتجر أولاً (معطّل افتراضياً)، وهذا البند لم يُفعَّل في أي بيئة إنتاج حقيقية بعد.
- [ ] صفحة "العملاء → الإحالات" في لوحة التحكم على جهاز Android حقيقي (تابلت أو هاتف كبير) — التحقق الحالي محاكاة أبعاد فقط.

## إجراء الاختبار الإنتاجي الموصى به
1. بعد أي نشر جديد على Railway، افتح الرابط الفعلي (مش localhost) من جهاز Android حقيقي.
2. نفّذ طلب تجريبي حقيقي بأقل قيمة ممكنة (أو استخدم حساب اختبار) وتابعه لحد التسليم.
3. راجع البنود 2 و3 فعلياً (كاميرا/إشعارات) — الاثنين محتاجين تفاعل مستخدم حقيقي (إذن متصفح) مش ممكن يتأكد منهم آلياً بالكامل.
4. سجّل أي مشكلة تظهر هنا كـ issue منفصل بدل ما تتجاهل، حتى لو بسيطة.
