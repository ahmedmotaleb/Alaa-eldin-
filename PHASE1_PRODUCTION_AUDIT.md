# تدقيق ما قبل التطوير — Phase 1

تدقيق فعلي مبني على فحص الكود الحالي في فرع `main` (commit `b140d3e` وقت كتابة هذا الملف)،
مش على الوثائق فقط. كل تصنيف هنا مدعوم بملف/جدول/endpoint حقيقي تم فحصه مباشرة.

**تصنيفات مستخدمة**: `مكتمل` (Complete) · `جزئي` (Partial) · `غير موجود` (Missing) ·
`محتاج إعداد إنتاج` (Needs production configuration) · `محتاج اختبار جهاز حقيقي` (Needs real device testing)

---

## Phase 2 — تجربة تسوق العميل

### A. شوهد مؤخراً (Recently Viewed)
**الحالة: غير موجود بالكامل.** لا يوجد أي كود لـ `recently viewed` أو `شوهد مؤخراً` في
`src/` (تم البحث بالنص الكامل). لا يوجد جدول أو localStorage key لهذا الغرض.
**يجب بناؤه من الصفر**: تخزين محلي (localStorage) لقائمة IDs بحد أقصى معقول، إزالة تكرار،
إعادة استيثاق (resolve) السعر/التوفر الحالي وقت العرض عبر endpoint فهرسة موجود بالفعل
(`resolveProducts` في `catalogService.ts` — نفس الدالة المستخدمة في `frequentlyPurchasedService.ts`
أدناه، قابلة لإعادة الاستخدام مباشرة).

### B. اشتريها تاني (Buy Again)
**الحالة: مكتمل فعلياً تحت اسم مختلف.** الميزة موجودة بالكامل كـ "المنتجات المتكررة"
(Frequently Purchased):
- `server/src/services/frequentlyPurchasedService.ts` — `listFrequentlyPurchased(userId)`:
  ترتيب حقيقي بـ `COUNT(*) DESC, SUM(quantity) DESC` من `order_items`/`orders` الحقيقية،
  يستثني الطلبات الملغاة (`status != 'cancelled'`)، يفلتر أي منتج `!available` بعد
  استيثاق السعر/التوفر الحالي عبر `resolveProducts`.
- Endpoint: `GET /api/account/frequently-purchased` (`server/src/routes/frequentlyPurchased.ts`).
- UI: `src/pages/AccountPage.tsx` سطر 86-90 — تُعرض في صفحة "حسابي" فقط، ليست في الصفحة
  الرئيسية.
- الكارت المستخدم (`ProductGridCard.tsx`) يحتوي بالفعل على `AddControl` (إضافة سلة بضغطة
  واحدة) و`FavoriteButton`.

**الفجوة الوحيدة الحقيقية**: الموضع (مش في الصفحة الرئيسية) والتسمية (`ar.account.frequentlyPurchasedTitle`
مش "اشتريها تاني" حرفياً). هذه تسمية/موضع فقط — لا يوجد داعٍ لإعادة بناء المنطق. **جزئي** (منطق مكتمل، عرض على الصفحة الرئيسية غير موجود).

### C. يشترى معه غالباً (Frequently Bought Together)
**الحالة: غير موجود بالكامل.** لا يوجد أي كود association-mining بين المنتجات مبني على
`order_items` التاريخية. الموجود حالياً في `ProductPage.tsx` (سطر 126-154) هو:
1. `alternatives` — بدائل يدوية يحددها الأدمن (`productAlternativeService.ts`، جدول
   `product_alternatives` من Batch K2) — **ليست** مبنية على بيانات شراء فعلية.
2. "منتجات مشابهة" — على الأرجح بنفس القسم (category-based)، مش co-purchase.

يجب بناء دالة SQL جديدة حقيقية (`GROUP BY` على أزواج منتجات من نفس `order_id`، عتبة حد أدنى
لعدد مرات الظهور المشترك، استبعاد غير المتاح، `LIMIT` صريح) — **غير موجود**.

### D. أقسام رئيسية مخصصة (Personalized Home)
**الحالة: غير موجود.** لا يوجد `getHomeSections`/`homeSections` مخصص بالمستخدم. الصفحة
الرئيسية للمتجر (`src/pages/HomePage.tsx` — لم يُفحص بالتفصيل هنا لكن لا توجد أي إشارة
لتخصيص بالمستخدم في catalogService/CatalogContext) عامة لكل الزوار. **غير موجود**.

### E. تحسينات البحث
**الحالة: مكتمل إلى حد كبير.**
- `server/migrations/0014_search_indexes.sql`: `CREATE EXTENSION pg_trgm` — فهرسة trigram
  حقيقية لتحمّل الأخطاء الإملائية (fuzzy).
- `catalogService.ts` (سطر ~140-172): مطابقة على `name` (`ILIKE` + `%` عامل fuzzy فعلي
  عبر pg_trgm)، `brand`، `category.name`، ومطابقة باركود دقيقة. `NORMALIZE_SQL` يطبّع
  التشكيل/الاختلافات الإملائية العربية قبل المقارنة.
- ترتيب النتائج: `similarity(name)` DESC، ثم `order_count` — هذا فعلياً autocomplete سريع
  ومرتب بجودة.
- `src/utils/recentSearches.ts` — البحث الأخير محلي (localStorage) **موجود بالفعل ومكتمل**.
- **غير موجود**: مطابقة SKU في بحث المتجر (موجودة فقط في بحث لوحة التحكم `adminProducts.ts`
  — هذا منطقي لأن SKU داخلي، أولوية منخفضة). **البحث الشائع (Popular Searches) وتسجيل
  استعلامات البحث المجمّع غير موجودين إطلاقاً** — لا يوجد جدول `search_log` أو ما يعادله.
  **جزئي** على مستوى الميزة الكاملة، **مكتمل** على الجزء الجوهري (fuzzy/typo-tolerance/autocomplete/recent).

### F. تحسينات صفحة المنتج
**الحالة: مكتمل جزئياً — أساس قوي جداً موجود بالفعل.**
- **مكتمل**: معرض صور حقيقي مع lightbox/fullscreen (`src/components/ProductGallery.tsx`،
  pinch-zoom أصلي من غير كود لمس مخصص)، صورة رئيسية (`isPrimary`)، شارة حالة مخزون
  (`available`/`low_stock`/`unavailable` — **بدون كمية دقيقة**، مطابق تماماً لمتطلب "لا تعرض
  الكمية الدقيقة")، وحدة، سعر حالي وسعر قديم (`oldPrice`) مع خصم محسوب، بدائل، منتجات
  مشابهة، تحكم كمية (`AddControl`)، زر مفضلة (`FavoriteButton`).
- **غير موجود على صفحة المنتج تحديداً**: "يشترى معه غالباً" (انظر C)، وزر "أضف لقائمة
  التسوق" (شوبينج ليست موجودة كميزة كاملة منفصلة لكن غير مربوطة بصفحة المنتج مباشرة — لم
  يوجد `shoppingList` في `ProductPage.tsx`).

---

## Phase 3 — متغيرات المنتج (Variants)

**الحالة: غير موجود بالكامل — هذا أكبر فجوة معمارية في كل القائمة.**
تم البحث الشامل عن `variant` في كل قاعدة الكود؛ الاستخدام الوحيد الموجود هو "variant"
بمعنى نسخة بانر (`adminBanners.ts` — `?variant=mobile` لصورة بانر مخصصة للموبايل)، **لا
علاقة له بمتغيرات المنتج إطلاقاً**.

الجدول `products` (من `0001_init.sql`) منتج واحد = صف واحد = سعر/تكلفة/باركود/SKU/مخزون
واحد. **كل الطلب في Phase 3 (منتج أب + متغيرات، SKU/باركود/سعر/مخزون مستقل لكل متغير،
السلة تخزن المتغير، لقطة الطلب تحفظ تفاصيل المتغير، الاستلام يستقبل متغير، التحليلات
تفرّق بين المتغيرات) غير موجود إطلاقاً ويحتاج بناء كامل.**

**هذا أخطر Phase من ناحية الهجرة** — أي تصميم يجب أن:
1. يبقي جدول `products` كما هو (كل منتج حالي = "متغير افتراضي" ضمنياً، بدون أي INSERT/UPDATE
   مدمر على البيانات الحالية).
2. لا يغيّر `order_items`/`stock_movements`/`product_cost_history`/`purchase_order_items`
   الحالية بأي شكل يكسر التاريخ.
3. أفضل نهج: جدول `product_variants` جديد + عمود اختياري `variant_id` على الجداول
   المرتبطة (nullable، NULL = "المنتج الأساسي نفسه بدون متغيرات" للحفاظ التام على التوافق
   الخلفي)، مش تعديل مدمر لأي عمود موجود.

---

## Phase 4 — اعتماد الاستبدال (Substitution Approval)

**الحالة: جزئي — أساس التجهيز موجود، الجزء الأهم (تفضيل العميل + لقطة الاستبدال + موافقة آمنة) غير موجود.**

**موجود** (`server/migrations/0036_picking_notes_delivery_instructions.sql`):
- `order_items.picked_status` — CHECK IN (`pending`, `picked`, `substituted`, `unavailable`).
- `order_items.picked_note` — ملاحظة حرة من الشخص المجهّز.
- عرض للعميل عبر صفحة التتبع/الإيصال (Phase 100) — الشارة موجودة.

**غير موجود بالكامل**:
- لا يوجد أي عمود لتفضيل العميل وقت الدفع (استبدل بمنتج مشابه / اتصل بي أولاً / احذف
  المنتج) — لا على مستوى الطلب ولا على مستوى الصنف.
- لا يوجد `original_product_id`/`replacement_product_id`/`replacement_quantity`/
  `replacement_price`/`reason`/`selected_by_user_id`/`selected_at` — الاستبدال الحالي مجرد
  **حالة نصية** (`substituted`) + ملاحظة حرة، بدون ربط فعلي بمنتج بديل حقيقي في القاعدة.
- **لا يوجد نظام موافقة آمن (approval token) إطلاقاً** — لا جدول، لا مسار، لا صفحة عامة.
  هذا يحتاج بناء كامل: توكن عالي الإنتروبيا (نفس نمط `tracking_token` الموجود في
  `orders` من ميزة التتبع الحالية — قابل لإعادة الاستخدام كنمط)، مرحلة نهائية تمنع
  الموافقة بعدها، تسجيل تدقيقي كامل.

**تعارض محتمل**: إعادة حساب السعر يجب أن يمر عبر نفس منطق `pricingService.ts`/
`orderService.ts` الحالي — **يجب عدم** بناء منطق تسعير جديد ومستقل بيتجاوز السيرفر
الحالي (خطر تكرار منطق).

---

## Phase 5–8 — الولاء، الإحالة، السلة المهجورة، التنبيه عند التوفر

**الحالة: غير موجود بالكامل للأربعة.** تم البحث الشامل عن `loyalty`، `referral`، `abandon`،
`back_in_stock`/`waitlist`/`notify_when_available` في كامل الكود والهجرات — **صفر نتائج**
لكل الأربعة. هذه ميزات جديدة بالكامل، غير موجودة ولا حتى بشكل جزئي.

ملاحظات تصميم مهمة قبل البناء (بناءً على الأنماط الموجودة فعلياً في المشروع):
- **الولاء (5)**: يجب أن يكون ledger فعلي (مطابق تماماً لنمط `product_cost_history`
  الموجود بالفعل — سجلات غير قابلة للتعديل، مش رقم واحد قابل للتحرير) وليس عمود
  `loyalty_balance` على `users`. منح النقاط مربوط بحالة طلب محددة (`delivered` — نفس
  الحالة المستخدمة بالفعل في CHECK constraint الطلبات).
- **الإحالة (6)**: يجب الربط بنفس آلية الخصومات الموجودة (`discounts` table) لو المكافأة
  كوبون، أو نظام الولاء الجديد (5) لو نقاط — تجنب بناء نظام مكافآت ثالث منفصل.
- **السلة المهجورة (7)**: يجب احترام `notification_preferences` الموجودة بالفعل
  (`order_updates`/`promotions`) — قناة الإشعار (Push موجود، WhatsApp موجود) يجب إعادة
  استخدامها بنفس آلية الإرسال الحالية (`whatsappService.ts`/`pushService.ts`)، **ليس**
  بناء آلية إرسال جديدة.
- **التنبيه عند التوفر (8)**: يعتمد على تغيّر `products.available`/`stock` — يمكن اكتشافه
  في نفس نقطة تحديث المخزون الحالية (`inventoryService.ts`/`goodsReceivingService.ts`)
  بدل polling منفصل.

---

## Phase 9 — جاهزية WhatsApp للإنتاج

**الحالة: البنية التحتية مكتملة، الجزء المتبقي محتاج إعداد إنتاج + توسعة بسيطة.**
- `server/src/services/whatsappService.ts` + `whatsapp_templates` + `whatsapp_messages`
  (Batch L9) — إدارة قوالب فعلية، سجل رسائل بحالة/خطأ.
- `adminIntegrationsRouter` (Phase 101، commit `ce3cacf`) — يعرض configured/not-configured،
  اختبار اتصال حقيقي (`POST /api/admin/integrations/whatsapp/test`) يستخدم
  `sendWhatsAppMessage()` الفعلي (مش محاكاة)، **لا يعرض التوكن أبداً** (تم التحقق).
  آخر نتيجة اختبار/آخر رسالة ناجحة **غير معروضة صراحة حالياً** في شاشة التكاملات — تحتاج
  إضافة بسيطة (query على `whatsapp_messages` الموجود بالفعل، مش عمود جديد).
- **غير موجود**: قوالب مُعدّة سلفاً (seeded) للمراحل الثمانية المطلوبة (تم إنشاء
  received/confirmed/preparing/... إلخ) — الجدول والـ CRUD موجودان، لكن لا توجد صفوف
  حقيقية مُنشأة لكل مرحلة. **محتاج عمل إداري (إنشاء قوالب) أكتر من كود.**
- **محتاج إعداد إنتاج**: بيانات اعتماد Meta الحقيقية غير مضبوطة في Railway production
  حالياً (تم التأكد من هذا في `PRODUCTION_CHECKLIST.md` — `WHATSAPP_ACCESS_TOKEN`/
  `WHATSAPP_PHONE_NUMBER_ID` غير موجودين في قائمة متغيرات الإنتاج الفعلية وقت آخر فحص).
- الرابط اليدوي الحالي (wa.me link في صفحة الطلب على الأرجح) — لم يُفحص مباشرة هنا، لكن
  لا يوجد أي تغيير مقترح عليه في هذا التدقيق، **يجب التأكد إنه يفضل شغال** أثناء أي تعديل.

## Phase 10 — جاهزية Web Push للإنتاج

**الحالة: مكتمل من ناحية آلية العمل الأساسية، محتاج توسعة أنواع الأحداث.**
- `push_subscriptions` + `notification_preferences` (Batch L10)، توليد VAPID تلقائي في
  السيرفر (لا يحتاج توكن خارجي — ميزة معمارية جيدة موجودة بالفعل).
- Phase 102 (commit `5bbd7bf`): طلب الإذن **لا يحدث تلقائياً عند تحميل الصفحة** —
  مرتبط بإجراء صريح (زر "تفعيل الإشعارات") فقط — **مطابق تماماً** لمتطلب Phase 10 بدون
  أي تعديل مطلوب.
- **غير موجود**: أنواع أحداث لموافقة الاستبدال، التنبيه عند التوفر، السلة المهجورة —
  لأن هذه الميزات نفسها غير موجودة بعد (Phase 4/7/8). حالة الطلب (order status) موجودة
  كنوع حدث بالفعل عبر `notification_preferences.order_updates`.

---

## Phase 11 — طباعة ملصقات الباركود

**الحالة: غير موجود بالكامل.** لا يوجد أي كود لتوليد ملصقات طباعة (`thermal`/`label print`/
`40x30`) في كامل المشروع. يوجد فقط:
- توليد SKU تلقائي (`productSkuService.ts` — `ALA-XXXXXX` عبر sequence حقيقي في القاعدة)
  — **هذا نمط توليد فريد قابل لإعادة الاستخدام** لتوليد باركود داخلي فريد أيضاً (لكن
  الباركود الداخلي المولّد يجب ألا يكتب فوق باركود مُصنّع حقيقي موجود بالفعل — نفس القاعدة
  المطبّقة بالفعل في `generateSkuForProduct` اللي بيرفض الكتابة فوق SKU موجود).
- صفحة مسح الباركود الإدارية (`BarcodeScanPage.tsx`، Phase 102) — قراءة فقط، **مفيش أي
  طباعة**.

يحتاج بناء كامل: صفحة طباعة جديدة، اختيار مقاس ليبل، اختيار منتج/متغيرات (بعد Phase 3)
ونسخ متعددة.

---

## Phase 12 — محرك العروض الترويجية

**الحالة: جزئي — أساس كوبونات بسيط فقط، أغلب المتطلبات غير موجودة.**
`discounts` (من `0001_init.sql`، بدون أي توسعة لاحقة غير تحويل الأنواع الرقمية/الطوابع
الزمنية): `code` (PK)، `type` (`percentage`|`fixed` فقط)، `value`، `min_order`، `max_uses`،
`used_count`، `active`، `expires_at`. صلاحية RBAC: `discounts.manage`.

**غير موجود من القائمة كاملة**: خصم فئة، خصم منتج، Buy X Get Y، سعر Bundle، خصم كمية، خصم
أول طلب، توصيل مجاني كعرض، حد استخدام لكل عميل (يوجد `max_uses` إجمالي فقط، مش لكل
عميل)، جدولة بداية (يوجد `expires_at` فقط بدون `starts_at`)، تتبع استخدام لكل عميل.

**السيرفر هو مصدر الحقيقة بالفعل** (لا يوجد أي كود يقبل خصم محسوب من العميل — هذا الجزء
الأمني سليم ويجب الحفاظ عليه بالضبط في أي توسعة).

---

## Phase 13 — تحسينات المخزون

**الحالة: النظام الأساسي قوي جداً بالفعل (مطابق لوصف المستخدم) — فجوات محددة فقط.**
`stock_movements.type` تطور عبر 3 هجرات: `restock`, `return`, `damage`, `loss`,
`adjustment`, `sale`, `cancel_restore`, `expired`, `supplier_return` (من
`0026_fefo_and_write_offs.sql`). **كل حركة مخزون تمر عبر سبب مسجّل بالفعل — لا يوجد أي
تعديل مباشر بدون سبب** (تم التحقق، مطابق تماماً لمتطلب "auditable stock movement").

- `purchase_order_items.ordered_qty` مقابل `received_qty` — **موجودان بالفعل**، يعني
  اكتشاف "استلام ناقص من المورد" ممكن حسابياً الآن (مقارنة الرقمين)، لكن **لا يوجد تقرير/
  شاشة مخصصة لفروق الاستلام (discrepancy report)** ولا **تباين سعر الشراء (price variance)**
  كميزة منفصلة — البيانات الخام موجودة، التحليل المخصص غير موجود. **جزئي**.
- **غير موجود**: أكواد سبب shrinkage منفصلة عن `loss` العام، تحويل بين مواقع (مرتبط
  بـ Phase 14 غير الموجود أصلاً)، حجز مخزون (stock reservation) وقت الدفع الفعلي —
  المخزون يُخصم مباشرة عند إنشاء الطلب حسب الكود الحالي في `inventoryService.ts` (لا يوجد
  حجز مؤقت منفصل).

---

## Phase 14 — جاهزية تعدد المواقع

**الحالة: غير موجود بالكامل، ولا يوجد حتى تجريد جزئي.** لا يوجد `location_id`/`warehouse`
في أي جدول. هذا متوقع ومقبول تماماً — المشروع بالكامل مبني على افتراض موقع واحد. تعليمات
المستخدم صريحة: **"لو التنفيذ الآن هيزعزع استقرار الإنتاج، جهّز فقط الـ schema/abstraction
واتركه معطّل افتراضياً."** التوصية: **تأجيل هذا الـ Phase بالكامل** لآخر القائمة، ولو
اتنفذ، ينفذ فقط كـ schema إضافي معطّل (feature-flagged)، صفر تأثير على الإنتاج الحالي.

---

## Phase 15 — تجربة استخدام لوحة التحكم

**الحالة: جزئي.**
- **غير موجود**: بحث شامل/command palette (`GlobalSearch`/`CommandPalette`) — تم البحث
  والتأكيد، صفر نتائج.
- `GET /api/admin/analytics/home-summary` **موجود بالفعل** (`adminAnalyticsRouter`،
  `getHomeSummary()` في `analyticsService.ts`) لكنه محدود حالياً على: إيرادات اليوم، عدد
  طلبات اليوم، إجمالي الطلبات، طلبات جديدة (`status='placed'`) فقط — **لا يغطي** أي من
  الاختصارات الثمانية المطلوبة (تجهيز/مخزون منخفض/نفاد/صلاحية/استرجاع/مشتريات معلقة/كاش
  مناديب/مشاكل تكامل). **لكن كل بيانات هذه الاختصارات موجودة بالفعل ومحسوبة سيرفرياً في
  أماكن أخرى** (`alertsService.ts` بالضبط — انظر Phase 16 أدناه) — يحتاج فقط دمج/توسعة
  `home-summary` الموجود، **ليس بناء بنية تحتية جديدة**.

---

## Phase 16 — مركز تنبيهات الإدارة

**الحالة: مكتمل جزئياً — أساس ممتاز موجود بالفعل ومطابق تماماً لمبدأ "لا تكرار".**
`server/src/services/alertsService.ts` — **بالضبط** النمط المطلوب: كل التنبيهات محسوبة حية
من الجداول الفعلية وقت الطلب (**لا يوجد جدول تنبيهات دائم مكرر** — تعليق صريح في الكود
نفسه يوضّح هذا القرار المعماري المتعمد، مطابق حرفياً لتعليمات المستخدم "Do not turn live
computed state into duplicate permanent alert rows"). 3 مستويات خطورة (`info`/`warning`/
`critical`) بدل 3 المطلوبة (`critical`/`high`/`medium`) — تسمية فقط، قابلة لإعادة
التسمية/إعادة التخطيط بسهولة.

**7 فئات موجودة بالفعل**: `low_stock`, `expired_batches`, `expiring_soon`, `stuck_orders`,
`pending_customer_returns`, `pending_supplier_returns`, `unsettled_rider_cash`.

**غير موجود من القائمة المطلوبة**: فشل معالجة طلب، فشل تكامل (موجود بشكل منفصل في شاشة
التكاملات لكن غير مدمج هنا كتنبيه)، مشكلة قاعدة بيانات/صحة (موجودة منفصلة في `/health`)،
مخزون غير متسق، نفاد كامل (out of stock كفئة منفصلة عن low_stock)، فشل إرسال WhatsApp
(البيانات موجودة في `whatsapp_messages.status='failed'` لكن غير مدمجة هنا).

---

## Phase 17 — دعم العملاء

**الحالة: غير موجود بالكامل.** لا يوجد `support_ticket`/`ticket` في أي مكان بالكود أو
الهجرات. يوجد `order_notes` (ملاحظات داخلية إدارية فقط، غير مرئية للعميل، من Batch L18) —
**هذا مختلف تماماً** عن تذكرة دعم يرفعها العميل نفسه. يحتاج بناء كامل: جدول، أذونات RBAC
جديدة، صفحة عميل، صفحة إدارة.

---

## Phase 18 — التحليلات

**الحالة: قوي جداً وموجود بالفعل لمعظم البنود — بعض المقاييس التسويقية غير موجودة لأن
ميزاتها الأساسية غير موجودة بعد.**
موجود بالفعل (`analyticsService.ts` + `purchasingInventoryAnalyticsService.ts`، Batch F +
L17 + Phase 99): إيرادات، طلبات، متوسط قيمة الطلب، إيراد حسب الفئة/الشريحة الزمنية، أفضل
المنتجات، تحليل المناديب، تحليل الشحن/الاسترجاع، أيام التغطية (days of cover)، Dead
stock/fast-mover (Phase 99)، قيمة الصلاحية المنتهية، قيمة الشطب.

**غير موجود لأن الميزة الأساسية غير موجودة**: استخدام كوبون الولاء (Phase 5 غير موجود)،
تحويلات الإحالة (Phase 6 غير موجود)، استرداد السلة المهجورة (Phase 7 غير موجود).

**يحتاج فحص/تحقق إضافي**: معدل العميل الجديد مقابل المتكرر، معدل إعادة الشراء — لم يُفحص
مباشرة إن كان موجوداً بدقة أو يحتاج إضافة (خارج نطاق الفحص السريع هنا، **جزئي/يحتاج تأكيد**).

---

## Phase 19 — الأداء

**الحالة: مكتمل إلى حد كبير — تم بناؤه بشكل صريح ومتكرر عبر الجلسة بالكامل.**
Pagination موجود (orders، admin products، إلخ)، فهارس `pg_trgm` للبحث، تقسيم الكود على
مستوى المسارات (Batch I)، تحسين الصور (Cloudinary + lazy loading)، لا يوجد استخدام لـ
Redis (مطابق لتوصية "لا تضف Redis قبل حاجة مثبتة"). **لم يتم فحص خطط تنفيذ SQL (query
plans) فعلياً في هذا التدقيق** — بند "مراجعة خطط الاستعلامات الثقيلة عند الحاجة" **يحتاج
تحقق فعلي منفصل**، ليس مجرد افتراض.

---

## Phase 20 — الأمان

**الحالة: مكتمل ومبني بعناية شديدة عبر الجلسة (Batch L13 + جلسات لاحقة).** كلمات مرور
قوية، 2FA حقيقي (TOTP)، RBAC حبيبي (granular)، جلسات آمنة قاعدة-بيانات مع IP hash ثابت
(تم ضبط `SESSION_IP_HASH_SALT` في الإنتاج الفعلي هذه الجلسة)، فحص أصل CSRF صريح
(`csrfOriginCheck.ts`)، Helmet/CSP مخصص للمشروع فعلياً (`imgSrc` يسمح فقط بـ Cloudinary،
`scriptSrc: 'self'` بدون inline)، Rate limiting على تسجيل الدخول/استعادة كلمة المرور، سجل
تدقيق دائم (`audit_logs`). **كل هذا يجب الحفاظ عليه حرفياً بدون أي تعديل** أثناء تنفيذ أي
Phase تالٍ — أي endpoint جديد (خصوصاً Phase 4/5/6/12) يجب أن يستخدم نفس middleware
`requirePermission`/`requireAdmin` الموجود، **ليس منطق تفويض جديد منفصل**.

---

## Phase 21 — قواعد الأرقام العربية

**الحالة: مكتمل ومُطبّق بمركزية حقيقية.** أدوات تنسيق مركزية (`formatMoney`، أدوات التاريخ/
الوقت في `cairoDate.ts`) تضمن أرقام غربية (`0123456789`) في كل مكان — هذا كان محور عمل
كامل (Batch B، "Central number/date/phone formatting utilities"). **أي Phase جديد (خصوصاً
واجهات جديدة مثل الولاء/الدعم/طباعة الملصقات) يجب استخدام نفس الأدوات الموجودة حرفياً،
وليس `toLocaleString()` افتراضي أو تنسيق يدوي جديد** — هذا الخطر الحقيقي الوحيد هنا: كود
جديد ينسى استخدام الأداة المركزية الموجودة.

---

## Phase 22 — تحقق الدفع (Checkout Validation)

**الحالة: مكتمل ومطابق حرفياً للمواصفة المطلوبة.**
`server/src/phone.ts` و`src/utils/phone.ts` (نفس المنطق مكرر عمداً بين السيرفر والفرونت
إند للتحقق الفوري + الثقة الصفرية بمدخلات العميل):
```
const EGY_MOBILE_RE = /^01[0125][0-9]{8}$/
```
**مطابق حرفياً 100%** للنمط المطلوب في التعليمات. لا يوجد أي تطبيع صامت لأرقام غير صحيحة
إلى صحيحة (تم التحقق من الكود — الدالة ترفض أو تقبل، لا تُعدّل الرقم). الاسم/العنوان/
المحافظة مطلوبون بالفعل في تحقق الدفع الحالي (`orders.ts`/`checkoutValidation` من Batch D).
تاريخ التوصيل مطلوب فعلياً عند تفعيل تقويم التوصيل (Phase 94 هذه الجلسة).

*(ملحوظة: النص الأصلي المُرسل من المستخدم انقطع في منتصف Phase 22 — "delivery-calendar
configura..." — الجزء المتبقي من هذا الـ Phase لم يصل. القسم الموجود منه تم تدقيقه
بالكامل ومطابق.)*

---

## ملخص تنفيذي — ترتيب الأولوية المقترح للتنفيذ

بناءً على الفحص أعلاه، هذا هو الترتيب المنطقي (الأقل خطورة/الأكثر قيمة فورية أولاً):

| الأولوية | Phase | السبب |
|---|---|---|
| 1 | Phase 2A (شوهد مؤخراً) + 2C (يشترى معه غالباً) + إبراز 2B على الرئيسية | قيمة تسويقية فورية، صفر تغيير schema خطير |
| 2 | Phase 16 (توسعة التنبيهات) + Phase 15 (دمج home-summary) | البيانات كلها موجودة فعلياً، عمل دمج/واجهة فقط |
| 3 | Phase 4 (تفضيل استبدال + لقطة استبدال + موافقة آمنة) | يبني على نظام تجهيز موجود، مطلوب بشدة تشغيلياً |
| 4 | Phase 12 (توسعة محرك العروض) | يبني على `discounts` الموجود، لا نظام جديد بالكامل |
| 5 | Phase 3 (المتغيرات) | **الأخطر معمارياً** — يحتاج أكبر عناية بالهجرات والتوافق الخلفي |
| 6 | Phase 5+6 (ولاء + إحالة) | ميزتان جديدتان مترابطتان، يُفضّل تنفيذهما معاً |
| 7 | Phase 7+8 (سلة مهجورة + تنبيه توفر) | تعتمد على قنوات إشعار موجودة، منطق جديد بسيط نسبياً |
| 8 | Phase 9+10 (تجهيز واتساب/push فعلياً) | إعداد إنتاج + قوالب، ليس كود جديد كبير |
| 9 | Phase 11 (طباعة ملصقات) | ميزة معزولة، لا تتقاطع مع أي نظام آخر |
| 10 | Phase 13 (تحسينات مخزون إضافية) + Phase 17 (دعم عملاء) | تحسينات تشغيلية غير حرجة |
| 11 | Phase 14 (تعدد المواقع) | **مؤجل** بناءً على تعليمات المستخدم الصريحة ما لم يُطلب غير ذلك |
| مستمر | Phase 18-22 | مراجعة/تحقق مستمر مع كل Phase أعلاه، ليست عمل منفصل |

**كل Phase من الجدول أعلاه سيُنفذ بنفس انضباط الجلسة الحالية**: هجرة إضافية فقط (لا
تعديل على هجرة مُطبّقة)، اختبارات Postgres حقيقية، بناء التطبيقات الثلاثة، تحقق حي
(Playwright/curl) قبل أي commit، ثم دفع + تأكيد نشر Railway فعلي.
